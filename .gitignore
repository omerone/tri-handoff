node_modules/
.next/
out/
build/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local

playwright-report/
test-results/
.playwright/

.DS_Store
*.log
npm-debug.log*

caddy_data/
caddy_config/
pgdata/

# Playwright's saved sign-in state
e2e/.auth/
.vercel

# Deployment credentials — never commit. This repo is public.
ops/.secrets.env
# Belt and braces: this repository is public, and a backup identity is the one
# secret whose loss is silent — it decrypts every dump ever taken.
ops/*identity*
ops/*.age
ops/deploy_key
ops/deploy_key.pub
